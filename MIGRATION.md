# به‌روزرسانی نسخه‌ی نصب‌شده‌ی روبی (بدون حذف نصب)

اپلیکیشن روبی از قبل روی گوشی کاربران نصب است. این سند توضیح می‌دهد نسخه‌ی
جدید چگونه روی همان نصب به‌روزرسانی می‌شود، چرا کاربر مجبور نیست برنامه را حذف
و دوباره نصب کند، و داده‌هایش چطور سالم می‌ماند.

---

## ۱. چرا به‌روزرسانی «بدون حذف نصب» کار می‌کند؟

اندروید هنگام نصب نسخه‌ی تازه روی نسخه‌ی قدیمی، **فقط** در این شرایط اجازه‌ی
به‌روزرسانی می‌دهد:

| شرط | مقدار در روبی | وضعیت |
| --- | --- | --- |
| `applicationId` یکسان | `com.ruby.factor_ruby` (ثابت در `android/app/build.gradle.kts` و `AppConstants.androidApplicationId`) | ✅ |
| امضای دیجیتال یکسان | همان keystore نسخه‌های قبلی (`RELEASE_KEYSTORE_BASE64`) | ✅ با بررسی خودکار در CI |
| `versionCode` بزرگ‌تر | هر نسخه عدد `build` را در `pubspec.yaml` بالا می‌برد (۱.۰.۸+۱۰) | ✅ |
| `minSdk` بالاتر نرود | `minSdk = 21` (پایین‌تر از همه‌ی نسخه‌های منتشرشده) | ✅ |

داده‌های کاربر هم با به‌روزرسانی **پاک نمی‌شوند**، چون در حافظه‌ی خود برنامه
ذخیره شده‌اند و اندروید آن‌ها را حفظ می‌کند:

* `SharedPreferences` → کلیدهای `ruby_*_v1` (کاربر، کسب‌وکار، تنظیمات، فاکتورها،
  مشتریان، کالاها، کارت‌ها، پیش‌نویس، تامین‌کنندگان، هزینه‌ها)
* فایل SQLite → `factor_ruby.sqlite` در پوشه‌ی اسناد برنامه

قاعده‌ی طلایی پروژه: **کلیدهای ذخیره‌سازی و `applicationId` هرگز تغییر نمی‌کنند.**
افزودن فیلد تازه فقط با مقدار پیش‌فرض در `fromMap` انجام می‌شود، نه با تغییر نام
کلید.

---

## ۲. چه چیزی هنگام اولین اجرای نسخه‌ی تازه اتفاق می‌افتد؟

مسیر فایل‌ها:

```
lib/main.dart
  └── AppMigration.instance.runOnStartup(database: AppDatabase())
        ├── دفتر مهاجرت: ruby_migration_ledger_v1
        ├── گام ۱: یکسان‌سازی ساختار داده‌های ذخیره‌شده
        ├── گام ۲: تکمیل قیمت خرید و سود فاکتورهای قبلی
        ├── گام ۳: ساخت کارت حساب تامین‌کنندگان و بازیابی مشتریان از فاکتورها
        ├── گام ۴: ترمیم شناسه‌ها، تاریخ‌ها و مبلغ‌های نامعتبر
        └── گام ۵: ارتقای جدول‌های SQLite و آینه‌کردن داده
```

مهاجرت **پیش از** ساخته شدن رابط کاربری `runApp` اجرا می‌شود تا هیچ صفحه‌ای
داده‌ی نیمه‌مهاجرت‌شده نبیند.

### تضمین‌های ایمنی

1. **پشتیبان خودکار:** پیش از اولین تغییر، یک فایل
   `ruby-upgrade-backup-v1-to-v2-<timestamp>.json` در پوشه‌ی اسناد برنامه ساخته
   می‌شود (۵ پشتیبان آخر نگه داشته می‌شود). مسیر آن در
   `ruby_last_upgrade_backup_v1` ذخیره می‌شود و از
   «تنظیمات ← انتقال داده‌ها به نسخه‌ی جدید» قابل بازیابی است.
2. **دفتر مهاجرت (ledger):** بعد از هر گام موفق،
   `ruby_migration_ledger_v1` ذخیره می‌شود؛ اگر برنامه وسط کار بسته شود،
   اجرای بعدی از همان گام ادامه می‌دهد.
3. **گام‌های بی‌اثر (idempotent):** اجرای دوباره‌ی هر گام، مقدارها را دو بار
   حساب نمی‌کند (مثلاً مانده‌حساب تامین‌کننده دوباره اضافه نمی‌شود).
4. **هیچ حذفی وجود ندارد:** در گام‌ها `DELETE`/`DROP` نداریم؛ رکورد تکراری
   شناسه‌ی تازه می‌گیرد، نه اینکه حذف شود.
5. **خطا برنامه را از کار نمی‌اندازد:** هر خطا در گزارش مهاجرت ثبت می‌شود،
   برنامه با داده‌ی فعلی بالا می‌آید و گام ناموفق در اجرای بعدی تکرار می‌شود.
6. **بدون قفل شدن:** کل مهاجرت سقف زمانی ۲۵ ثانیه دارد
   (`AppMigration.startupTimeout`).

### نتیجه‌ی مهاجرت برای کاربران فعلی

* فاکتورها، مشتریان، کالاها، کارت‌ها و پیش‌نویس‌ها دست‌نخورده می‌مانند.
* برای فاکتورهای قدیمی، قیمت خرید اقلام از روی کاتالوگ پر می‌شود و
  «بهای تمام شده» و «سود» محاسبه می‌شود (پیش از این صفر بود).
* از فاکتورهای خرید، کارت حساب تامین‌کننده ساخته و مانده‌حساب آن‌ها محاسبه
  می‌شود.
* اگر مشتری‌ای در فهرست مشتریان نباشد اما فاکتور داشته باشد (حالتی که در
  نسخه‌های قبلی به‌خاطر بازنویسی فهرست با لیست خالی پیش می‌آمد)، مشتری از روی
  فاکتورهایش بازسازی می‌شود و مانده‌حساب او از فاکتورهای پرداخت‌نشده محاسبه
  می‌گردد؛ فاکتورهایش هم به همان مشتری وصل می‌شوند. نام‌های پیش‌فرضی مثل
  «مشتری عمومی» کارت حساب نمی‌سازند.
* جدول‌های SQLite با `PRAGMA user_version` ارتقا پیدا می‌کنند و ستون‌های تازه با
  `ALTER TABLE ... ADD COLUMN` اضافه می‌شوند؛ ردیف‌های قبلی حفظ می‌شوند.

---

## ۳. نسخه‌ی داده و نقشه‌ی راه

| نسخه‌ی برنامه | نسخه‌ی داده (`PrefsStore.schemaVersion`) | تغییر مهم |
| --- | --- | --- |
| ۱.۰.۴ تا ۱.۰.۷ | ۱ | ساختار اولیه |
| ۱.۰.۸+ | ۲ | سود/بهای تمام شده، تامین‌کنندگان، هزینه‌ها، جدول‌های تازه‌ی SQLite |

افزودن گام تازه در آینده: یک `MigrationStep` با `id` بزرگ‌تر در
`lib/core/migration/migration_steps.dart` اضافه کنید و `PrefsStore.schemaVersion`
را بالا ببرید. گام باید بی‌اثر باشد.

---

## ۴. پشتیبان‌گیری و بازیابی

* **پشتیبان دستی:** تنظیمات ← «پشتیبان‌گیری و بازیابی محلی» (فایل JSON).
  بازگردانی پشتیبان نسخه‌های قدیمی هم پشتیبانی می‌شود؛ پس از بازگردانی،
  مهاجرت دوباره اجرا می‌شود تا فیلدهای تازه پر شوند.
* **بازیابی پشتیبان پیش از به‌روزرسانی:** تنظیمات ← «انتقال داده‌ها به نسخه‌ی
  جدید» ← «بازیابی داده‌های پیش از به‌روزرسانی».
* پشتیبان خام شامل همه‌ی کلیدهای `SharedPreferences` است (حتی رکوردهای ناقص)،
  بنابراین هیچ داده‌ای در فرایند مهاجرت از دست نمی‌رود.

---

## ۵. بررسی خودکار پیش از انتشار

اسکریپت `scripts/verify_upgrade_safety.sh` روی APK ساخته‌شده این‌ها را کنترل
می‌کند:

1. `applicationId` همان `com.ruby.factor_ruby` باشد.
2. `versionName`/`versionCode` با `pubspec.yaml` بخواند.
3. اگر APK نسخه‌ی قبلی در دسترس باشد:
   * `versionCode` بزرگ‌تر باشد،
   * `minSdk` بالا نرفته باشد،
   * اثر انگشت SHA-256 گواهی امضا یکسان باشد.
4. برای خروجی release، `android:debuggable` نباشد.

این بررسی در دو جا اجرا می‌شود:

* `.github/workflows/ci.yml` روی هر push این برنچ (تحلیل، تست‌ها و APK دیباگ).
* `.github/workflows/release.yml` روی نسخه‌ی release؛ پیش از ساخت GitHub Release،
  APK آخرین نسخه‌ی منتشرشده را دانلود می‌کند و امضا/شماره‌ی نسخه را مقایسه
  می‌کند. اگر امضا متفاوت باشد، ساخت متوقف می‌شود؛ چون در آن حالت کاربران
  مجبور به حذف نصب و از دست دادن اطلاعات می‌شوند.

اجرای دستی:

```bash
EXPECTED_VERSION_CODE=10 REQUIRE_NON_DEBUGGABLE=true \
  scripts/verify_upgrade_safety.sh build/app/outputs/flutter-apk/app-release.apk
```

---

## ۶. تست‌ها

```bash
flutter test
```

* `test/migration_test.dart` — داده‌ی واقعی نسخه‌ی ۱.۰.۴ را می‌سازد و بررسی
  می‌کند که پس از مهاجرت: هیچ رکوردی حذف نشده، سود و بهای تمام شده محاسبه شده،
  تامین‌کننده ساخته شده و اجرای دوباره داده را تغییر نمی‌دهد.
* `test/persistence_test.dart` — تضمین می‌کند تغییر کاربر پیش از پایان خواندن
  اولیه گم نمی‌شود و مبلغ‌ها (مانده‌حساب، پرداخت فاکتور) دو بار حساب نمی‌شوند.
