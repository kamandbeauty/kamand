# تغییرات فاکتور ساز روبی

## ۱.۰.۸+۱۰ — به‌روزرسانی امن نسخه‌ی نصب‌شده

### انتقال کامل همه‌ی تغییرات برنچ `arena/01a0bed4-kamand`

* گزارش سود و بهای تمام شده، گزارش ماهیانه و کارت حساب تامین‌کنندگان
* صفحه‌های تازه: حساب‌داری، بدهی‌ها، هزینه‌ها، تامین‌کنندگان
* سازگاری اندروید با صفحه‌ی ۱۶ کیلوبایت (compileSdk/targetSdk 36، NDK 28)

### مهاجرت داده‌ها (بدون حذف نصب و بدون از دست رفتن اطلاعات)

* لایه‌ی مهاجرت تازه در `lib/core/migration/`:
  * یکسان‌سازی ساختار رکوردهای ذخیره‌شده و افزودن فیلدهای تازه با مقدار پیش‌فرض
  * تکمیل قیمت خرید اقلام فاکتورهای قدیمی از روی کاتالوگ و محاسبه‌ی سود و
    بهای تمام شده
  * ساخت کارت حساب تامین‌کنندگان از فاکتورهای خرید و محاسبه‌ی مانده‌حساب
  * بازسازی مشتریانی که فاکتور دارند اما در فهرست مشتریان نیستند
  * ترمیم شناسه‌ها/تاریخ‌ها/مبلغ‌های نامعتبر بدون حذف هیچ رکوردی
  * ارتقای ساختار SQLite با `PRAGMA user_version` (افزایشی و بدون حذف داده)
* پشتیبان خودکار پیش از به‌روزرسانی + دفتر مهاجرت قابل‌ازسرگیری + بازیابی از
  تنظیمات
* اجرای مهاجرت پیش از ساخت رابط کاربری در `main()` و نمایش نتیجه به کاربر

### رفع ایرادهای داده‌ای (مهم برای کاربران فعلی)

* مانده‌حساب مشتری/تامین‌کننده و پرداخت فاکتورها دیگر دو بار اعمال نمی‌شود
  (در نسخه‌ی قبل به‌دلیل به‌روزرسانی خوش‌بینانه + اعمال دوباره پس از بارگذاری،
  مبلغ‌ها دو برابر می‌شدند).
* نوشتن روی حافظه پیش از پایان خواندن اولیه، دیگر داده‌ی ذخیره‌شده را بازنویسی
  نمی‌کند.
* نوار نسخه در تنظیمات از `AppConstants.appVersion` خوانده می‌شود.
* ویرایش فاکتور دیگر مانده‌حساب مشتری/تامین‌کننده را دوباره اضافه نمی‌کند؛
  فقط «اختلاف» با فاکتور قبلی اعمال می‌شود.

### CI و انتشار

* ورک‌فلوی `CI (analyze + tests + debug APK)` برای تحلیل، تست‌ها و ساخت APK
  دیباگ روی همین برنچ.
* اسکریپت `scripts/verify_upgrade_safety.sh` و اجرای آن در ورک‌فلوی انتشار:
  کنترل یکسان بودن `applicationId`، افزایش `versionCode`، یکسان بودن امضای
  دیجیتال با نسخه‌ی منتشرشده‌ی قبلی و بالاتر نرفتن `minSdk`.
* تست‌های تازه: `test/migration_test.dart` و `test/persistence_test.dart`.

## ۱.۰.۷+۹

* سازگاری اندروید ۱۶ کیلوبایت + اصلاح گزارش سود (برنچ `arena/01a0bed4-kamand`)

## ۱.۰.۶+۸

* اصلاح محاسبه‌ی سود ناخالص و بهای تمام شده، گزارش ماهیانه، کارت حساب
  تامین‌کننده، صفحات هزینه و بدهی‌ها

## ۱.۰.۴+۴

* نسخه‌ی منتشرشده با امضا و خروجی‌های AAB/APK تفکیک‌شده بر اساس ABI
