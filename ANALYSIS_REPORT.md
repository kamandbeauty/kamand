# گزارش جامع تحلیل فنی، معماری و بررسی مشکلات اپلیکیشن «فاکتور روبی» (Factor Ruby)

**تاریخ بررسی:** ۱۰ مرداد ۱۴۰۵ (10 August 2026)  
**نسخه مورد بررسی:** مخزن `kamand` (شامل کدهای Flutter/Dart موبایل و پیش‌نمایش وب React/Vite)

---

## ۱. معرفی و بررسی ساختار کلی مخزن (Project Structure & Overview)

مخزن «فاکتور روبی» از دو بخش مجزا و مکمل تشکیل شده است:
1. **اپلیکیشن موبایل (مسیرهای `lib/` و `android/`):** پیاده‌سازی‌شده با **Flutter & Dart**، با استفاده از الگوهای معماری چندلایه‌ای، مدیریت وضعیت با `flutter_riverpod` و دیتابیس محلی با `drift` (SQLite).
2. **پیش‌نمایش تعاملی وب (مسیر `web_preview/`):** پیاده‌سازی‌شده با **React 18، Vite و Tailwind CSS** جهت تجربه تعاملی جریان‌های اصلی رابط کاربری در مرورگر، با ذخیره‌سازی داده‌های نمونه در `localStorage`.

مستندات پروژه شامل `README.md`، `ARCHITECTURE.md` (شرح معماری و لایه‌ها) و `DATABASE.md` (جداول و شمای دیتابیس) است. تحلیل جامع کدها نشان می‌دهد که اگرچه طراحی رابط کاربری و جریان کاربری (UX/UI) بسیار مطلوب و با زبان فارسی و ساختار راست‌به‌چپ (RTL) هماهنگ است، اما در هر دو کدبیس (Flutter و React) مشکلات، تناقض‌ها و باگ‌های فنی قابل‌توجهی وجود دارد که در ادامه به تفصیل دسته‌بندی و تحلیل شده‌اند.

---

## ۲. مشکلات و باگ‌های اپلیکیشن موبایل فلاتر (`lib/` - Flutter Codebase Issues)

### الف) عدم اتصال لایه مدیریت وضعیت (Riverpod) به دیتابیس محلی (Drift SQLite)
* **مشکل:** در مستندات پروژه (`ARCHITECTURE.md` و `DATABASE.md`) صراحتاً قید شده است که سیستم به صورت **Offline-First** روی دیتابیس **SQLite / Drift** کار می‌کند و کلاس‌های دیتابیس در `lib/database/app_database.dart` پیاده‌سازی شده‌اند. با این حال، بررسی کدها نشان می‌دهد که کلاس `AppDatabase` در هیچ‌جای پروژه نمونه‌سازی (Instantiate) یا استفاده نشده است.
* **پیامد:** تمام پرووایدرهای مدیریت وضعیت در `lib/providers/` (شامل `InvoiceListNotifier`، `CustomerListNotifier`، `ProductListNotifier`، `UserNotifier`، `BusinessNotifier` و `SettingsNotifier`) داده‌ها را منحصراً در حافظه موقت (In-Memory Mock List) نگهداری می‌کنند. در نتیجه:
  - هرگونه افزودن فاکتور، مشتری یا کالا با بستن و باز کردن اپلیکیشن به کلی از بین می‌رود و برنامه به داده‌های پیش‌فرض بازمی‌گردد.
  - هیچ عملیات Persistent Storage (ذخیره‌سازی دائمی) روی دیتابیس Drift انجام نمی‌شود.

### ب) تناقض جدی میان مستندات دیتابیس (`DATABASE.md`) و کد پیاده‌سازی‌شده (`app_database.dart`)
1. **جدول اقلام فاکتور (`invoice_items`):** در مستندات `DATABASE.md` (جدول ۶)، جدول `invoice_items` به عنوان یک جدول رابطه‌ای مستقل با `FOREIGN KEY` به `invoices.id` و قابلیت `ON DELETE CASCADE` تعریف شده است. اما در فایل `app_database.dart`، جدول `InvoicesTable` اقلام فاکتور را به صورت یک رشته JSON داخل ستون `itemsJson` ذخیره می‌کند و جدول رابطه‌ای مجزایی برای اقلام وجود ندارد.
2. **جداول مفقوده (`payments` و `backups`):** در `DATABASE.md` جداول ۷ (`payments` - تاریخچه تراکنش‌های مالی و دریافت/پرداخت) و ۱۱ (`backups` - مدیریت نسخه‌های پشتیبان) مستند شده‌اند، اما در `app_database.dart` هیچ تعریفی برای این جداول وجود ندارد.
3. **ادغام جداول هزینه و درآمد (`expenses` و `income`):** در مستندات، هزینه‌ها و درآمدها در دو جدول مستقل ۸ و ۹ تعریف شده‌اند، اما در `app_database.dart` هر دو در یک جدول مشترک به نام `FinancialTable` (با ستون بولین `isIncome`) ادغام شده‌اند.

### ج) باگ‌های منطقی و مقادیر هاردکد (Hardcoded Values) در صفحه صدور فاکتور (`InvoiceCreateScreen.dart`)
* **هاردکد بودن مشتری و شماره کارت:** در متد `_saveInvoice()` هنگام ایجاد فاکتور جدید، شناسه مشتری به صورت ثابت `customerId: 'c1'` و شماره کارت واریزی به صورت ثابت `cardNumber: '6037-9975-1234-5678'` ست می‌شود؛ بنابراین حتی در صورت تایپ نام مشتری جدید، شناسه او همیشه به مشتری اول متصل است.
* **عدم به‌روزرسانی مانده‌حساب مشتری:** هنگام ذخیره فاکتور فروش غیرنقدی در `InvoiceCreateScreen`، هیچ فراخوانی به `customerListProvider` برای افزایش مانده بدهی مشتری انجام نمی‌شود.

### د) باگ‌های محاسباتی در داده‌های اولیه و داشبورد فلاتر
* **تناقض درصد تخفیف در فاکتور نمونه (`invoice_provider.dart`):** در فاکتور نمونه شماره `1003`، جمع کل اقلام `3,300,000` تومان و مبلغ تخفیف `165,000` تومان (معادل **۵ درصد**) است، اما ستون `discountPercent` به اشتباه مقدار `10` را نگهداری می‌کند.
* **فیلتر نشدن تاریخ در آمار امروز داشبورد (`dashboard_screen.dart`):** در خطوط ۳۱ و ۳۲ از `DashboardScreen.dart`، متغیرهای `todaySales` (فروش امروز) و `todayReceived` (دریافت امروز) بدون هیچ شرطی روی تاریخ، کل فاکتورهای موجود در تاریخچه را جمع می‌زنند.

---

## ۳. مشکلات و باگ‌های نسخه پیش‌نمایش وب ری‌اکت (`web_preview/` - React Codebase Issues)

### الف) باگ‌های منطقی و کارکردی (Logic & Functional Bugs)

| ردیف | فایل مربوطه | شرح مشکل و علت فنی | پیامد برای کاربر |
| :---: | :---: | :--- | :--- |
| **۱** | `FinancialView.jsx` | **بی‌اثر بودن فیلتر بازه زمانی در گزارش سود و زیان:** دکمه‌های «امروز»، «این هفته»، «این ماه» و «دلخواه» استیت `dateFilter` را تغییر می‌دهند؛ اما محاسبه مجموع درآمدها (`totalSalesRevenue`) و هزینه‌ها هیچ شرط فیلتری روی `inv.date` یا `e.date` ندارد. | کاربر با کلیک روی هر بازه زمانی، همواره اعداد یکسان (جمع کل تمام تاریخچه) را مشاهده می‌کند. |
| **۲** | `DashboardView.jsx` <br> `InvoiceCreatorView.jsx` | **تناقض فرمت تاریخ در آمار داشبورد و فاکتور جدید:** در داشبورد، تاریخ امروز به صورت `'1405/05/20'` (فرمت `YYYY/MM/DD`) تعریف شده است. اما در فرم ایجاد فاکتور جدید، تاریخ پیش‌فرض به صورت متنی `'دوشنبه ۱۹ مرداد ۱۴۰۵'` است (و از تابع `getTodayJalali()` استفاده نشده است). | فاکتورهای جدید ایجادشده توسط کاربر هرگز با شرط `inv.date === today` در داشبورد مطابقت ندارند و آمار «فروش امروز» و «دریافت امروز» صفر یا ثابت می‌ماند. |
| **۳** | `App.jsx` <br> `InvoiceCreatorView.jsx` | **باگ مدیریت چندزبانه (Multi-window / Factor Tabs Bug):** امکان افزودن تب‌های پیش‌فاکتور در نوار پایینی وجود دارد، اما فرم `InvoiceCreatorView` به استیت تب انتخابی (`activeTabId`) متصل نیست و اطلاعات فرم برای هر تب به صورت مستقل ذخیره نمی‌شود. | جابجایی بین تب‌های ۱، ۲ و ۳ هیچ تاثیری روی فرم ندارد و کاربر در تمام تب‌ها یک فاکتور در حال ویرایش یکسان را می‌بیند. |
| **۴** | `InvoiceCreatorView.jsx` | **عدم امکان انتخاب مشتری و کالا از کاتالوگ:** در کد صفحه، استیت `showCatalogModal` (خط ۶۲) و تابع `addFromCatalog` تعریف شده‌اند، اما در رابط کاربری هیچ دکمه‌ای برای باز کردن مدال انتخاب کالا از کاتالوگ یا انتخاب مشتری از لیست ثبت‌شده وجود ندارد. | کاربر ناچار است نام اقلام، قیمت‌ها و نام مشتری را در هر فاکتور به صورت دستی تایپ کند. |
| **۵** | `App.jsx` <br> `Navbar.jsx` | **عدم امکان دسترسی به مدال ابزارهای هوشمند (`SmartToolsModal`):** در `App.jsx` استیت `showSmartToolsModal` تعریف و کامپوننت رندر شده است، اما هیچ دکمه‌ای در هدر (`Navbar`) یا منوی کناری (`Sidebar`) برای فراخوانی آن وجود ندارد. در `Navbar.jsx` نیز پراپ `onOpenGlobalSearch` ارسال شده که کامپوننت هدر حتی آن را در آرگومان‌های خود دریافت نمی‌کند. | کاربران هرگز نمی‌توانند به پنجره ۱۵ قابلیت هوشمند، بکاپ/بازیابی JSON، انتقال به گوشی جدید و قفل PIN دسترسی پیدا کنند. |

---

### ب) خطاهای کدنویسی، سینتکس و رابط کاربری (Syntax / Runtime / UI Bugs)

1. **خطای استفاده از `this` در Functional Component ری‌اکت (`CustomerManagementView.jsx` خط ۴۳۱):**
   - **کد فعلی:** `this?.business?.shopName || 'فروشگاه روبی'`
   - **تحلیل خطا:** در کامپوننت‌های تابعی (Functional Components) ری‌اکت، کلمه کلیدی `this` تعریف‌نشده (`undefined`) است. این عبارت همیشه به `undefined` ارزیابی شده و به مقدار پیش‌فرض `'فروشگاه روبی'` فال‌بک می‌کند.
2. **استفاده از `window.history.back()` در هدر صفحه کالاها (`ProductManagementView.jsx` خط ۸۷):**
   - **کد فعلی:** `<button onClick={() => window.history.back()}>`
   - **تحلیل خطا:** این پروژه یک Single Page Application است که ناوبری صفحات آن توسط استیت `activeTab` کنترل می‌شود. فراخوانی `window.history.back()` باعث بازگشت در تاریخچه مرورگر کاربر (Browser History) شده و ممکن است کاربر را به کلی از سایت/برنامه خارج کند.
3. **عدم استفاده از تگ `<form>` در مرحله اول آنبوردینگ (`OnboardingModal.jsx`):**
   - **تحلیل خطا:** فیلد ورود نام کاربر در مرحله اول درون `<form onSubmit={...}>` قرار ندارد. در نتیجه با فشردن کلید Enter روی کیبورد، فرم سابمیت نمی‌شود و کاربر حتماً باید روی دکمه «بعدی» کلیک کند.
4. **ثابت بودن متن دکمه در مدال خرید اشتراک (`PricingPlansModal.jsx` خط ۱۵۹):**
   - **تحلیل خطا:** کاربر چه اشتراک ۱ ماهه، چه ۶ ماهه و چه ۱ ساله را انتخاب کند (`selectedPlan`)، متن دکمه پایین مدال همیشه به صورت ثابت **«فعالسازی یک ساله»** باقی می‌ماند.
5. **تناقض در شماره نسخه برنامه (Version Inconsistency):**
   - در فایل `README.md`، منوی کناری (`Sidebar.jsx`) و فایل `pubspec.yaml` نسخه برنامه **۵.۸.۰** درج شده است.
   - اما در صفحه تنظیمات (`SettingsView.jsx` خط ۳۹۷) نسخه برنامه **`۱.۰.۰ release`** و در فایل پشتیبان `SmartToolsModal.jsx` نسخه **`1.0.0`** نشان داده می‌شود.

---

## ۴. جدول خلاصه مشکلات و اولویت‌بندی (Summary & Priority Matrix)

| اولویت | بخش | عنوان مشکل | شدت تأثیر روی تجربه کاربری / پایداری |
| :---: | :---: | :--- | :--- |
| 🔴 **بحرانی (High)** | Flutter | عدم اتصال Riverpod Providers به دیتابیس Drift/SQLite | از دست رفتن ۱۰۰٪ اطلاعات کاربران موبایل پس از بستن برنامه |
| 🔴 **بحرانی (High)** | Flutter / Docs | مغایرت شمای جداول `DATABASE.md` با `app_database.dart` | بی‌نظمی در معماری دیتابیس و عدم وجود جداول Payments و Backups |
| 🔴 **بحرانی (High)** | Web Preview | عدم کارکرد فیلتر زمانی در گزارش مالی (`FinancialView`) | نمایش آمار نادرست و غیرواقعی سود و زیان دوره‌ای |
| 🔴 **بحرانی (High)** | Web Preview | تناقض فرمت تاریخ‌ها در ایجاد فاکتور و فیلتر داشبورد | صفر ماندن همیشگی آمار فروش و دریافت امروز در داشبورد وب |
| 🟡 **متوسط (Medium)** | Web Preview | باگ مدیریت استیت تب‌های چندپنجره‌ای پیش‌فاکتور | عدم امکان کار همزمان روی چند فاکتور متفاوت در تب‌ها |
| 🟡 **متوسط (Medium)** | Web Preview | در دسترس نبودن دکمه انتخاب کالا از کاتالوگ و مشتری از لیست | تایپ دستی و زمان‌بر اطلاعات در فرم صدور فاکتور |
| 🟡 **متوسط (Medium)** | Web Preview | عدم دسترسی به مدال ابزارهای هوشمند (`SmartToolsModal`) | مخفی ماندن قابلیت‌های بکاپ/بازیابی، قفل PIN و جستجوی سراسری |
| 🟡 **متوسط (Medium)** | Flutter | هاردکد بودن `customerId` و `cardNumber` در ذخیره فاکتور | ثبت اطلاعات نادرست مشتری و کارت در فاکتورهای جدید فلاتر |
| 🟢 **کم (Low)** | Web Preview | خطای `this?.business` در کامپوننت فانکشنال ری‌اکت | فال‌بک همیشگی به نام پیش‌فرض فروشگاه در پیامک یادآوری |
| 🟢 **کم (Low)** | Web Preview | استفاده از `window.history.back()` در هدر صفحه کالاها | خروج ناخواسته از برنامه با زدن دکمه بازگشت هدر |
| 🟢 **کم (Low)** | Web Preview / Docs | تناقض شماره نسخه در بخش‌های مختلف برنامه (`۵.۸.۰` در برابر `۱.۰.۰`) | سردرگمی کاربر در تشخیص نسخه واقعی برنامه |

---

## ۵. پیشنهادات اجرایی برای بهبود و رفع مشکلات (Recommendations)

1. **یکپارچه‌سازی لایه دیتابیس فلاتر با Riverpod:**
   - اتصال پرووایدرهای `InvoiceListNotifier`، `CustomerListNotifier` و `ProductListNotifier` به نمونه تک‌لتون `AppDatabase`.
   - استفاده از متدهای `select`، `insert`، `update` و `delete` در Drift جهت پایدارسازی واقعی داده‌ها در SQLite.
2. **اصلاح شمای دیتابیس مطابق مستندات `DATABASE.md`:**
   - ایجاد جدول مستقل `invoice_items` و افزودن جداول `payments` و `backups`.
   - تفکیک جداول `expenses` و `income` یا اصلاح مستندات مطابق ساختار فعلی `FinancialTable`.
3. **همگام‌سازی فرمت تاریخ‌ها در پیش‌نمایش وب:**
   - استفاده یکپارچه از فرمت استاندارد شمسی `YYYY/MM/DD` با کمک تابع `getTodayJalali()` در تمام کامپوننت‌ها (داشبورد، فرم فاکتور و گزارش مالی).
4. **پیاده‌سازی واقعی فیلتر تاریخ در `FinancialView.jsx`:**
   - اعمال تابع فیلتر روی آرایه `invoices`، `expenses` و `incomes` بر اساس انتخاب کاربر (امروز، هفته، ماه).
5. **تکمیل قابلیت چندزبانه و دکمه‌های کاتالوگ در `InvoiceCreatorView.jsx`:**
   - نگهداری آرایه‌ای از استیت‌های فرم به ازای هر `tab.id` در `App.jsx` و افزودن دکمه «انتخاب از کاتالوگ» برای باز کردن `showCatalogModal`.
6. **افزودن دکمه‌های ابزارهای هوشمند و جستجوی سراسری به هدر (`Navbar.jsx`):**
   - اضافه کردن دکمه‌های جستجو (`Search`) و قابلیت‌های هوشمند (`Sparkles`/`Wrench`) به نوار بالایی جهت دسترسی کاربران.
7. **یکپارچه‌سازی شماره نسخه (Version Consistency):**
   - تغییر تمامی متون نمایش‌دهنده نسخه در `SettingsView.jsx` و `SmartToolsModal.jsx` به نسخه رسمی **۵.۸.۰**.

---

## ۶. گزارش حل مشکلات و وضعیت کنونی (Fixes & Resolution Status)

در تاریخ ۱۰ مرداد ۱۴۰۵، **تمامی ۱۴ مشکل و باگ شناسایی‌شده** در هر دو بخش **وب پیش‌نمایش (React/Vite)** و **اپلیکیشن موبایل (Flutter/Dart)** به صورت تک‌به‌تک و دقیق حل و اصلاح شدند:

1. **یکپارچه‌سازی شماره نسخه:** شماره نسخه در فایل‌های `SettingsView.jsx` و `SmartToolsModal.jsx` به نسخه رسمی **۵.۸.۰** تغییر یافت.
2. **اصلاح باگ `this?.business` در `CustomerManagementView.jsx`:** پراپ `business` به صورت مستقیم از `App.jsx` به این کامپوننت ارسال شد و دسترسی به نام فروشگاه در متن پیامک یادآوری بدهی به `business?.shopName || 'فروشگاه روبی'` تغییر یافت.
3. **جایگزینی `window.history.back()` در `ProductManagementView.jsx`:** عملکرد دکمه بازگشت هدر به پراپ `onBack` متصل شد تا در معماری SPA کاربر را به تب داشبورد بازگرداند و از خروج ناخواسته مرورگر جلوگیری کند.
4. **داینامیک شدن متن دکمه در مدال اشتراک طلایی (`PricingPlansModal.jsx`):** متن دکمه به صورت پویا بر اساس استیت انتخابی کاربر (`selectedPlan`) میان «فعالسازی یک ساله»، «فعالسازی شش ماهه» و «فعالسازی یک ماهه» تنظیم شد.
5. **فعال‌سازی سابمیت با Enter در مرحله اول آنبوردینگ (`OnboardingModal.jsx`):** فیلد ورود نام داخل تگ `<form onSubmit={...}>` قرار گرفت تا فشردن Enter کاربر را مستقیماً به مرحله بعد هدایت کند.
6. **اصلاح و همگام‌سازی تاریخ شمسی و فیلترها (`DashboardView` و `FinancialView`):**
   - در داشبورد وب (`DashboardView.jsx`) و فرم صدور فاکتور (`InvoiceCreatorView.jsx`)، تاریخ امروز به صورت پویا با تابع `getTodayJalali()` تنظیم شد تا فاکتورهای جدید در آمار «فروش امروز» و «دریافت امروز» داشبورد به‌درستی محاسبه و نمایش داده شوند.
   - در گزارش مالی (`FinancialView.jsx`)، فیلتر بازه‌های زمانی («امروز»، «این هفته»، «این ماه») به‌طور واقعی روی آرایه‌های فاکتورها، درآمدها و هزینه‌ها اعمال شد.
7. **حل باگ استیت چندپنجره‌ای پیش‌فاکتور (`Multi-window Factor Tabs`):** در `App.jsx` یک دیکشنری مجزا (`tabInvoiceStates`) به ازای شناسه هر تب ایجاد شد و کامپوننت `InvoiceCreatorView` به `key={activeFactorTabId}` مجهز شد تا با جابجایی بین تب‌های ۱، ۲ و ۳، اطلاعات ویرایش‌شده هر فاکتور به صورت مستقل حفظ و بازیابی شود.
8. **اضافه شدن دکمه‌های «انتخاب از کاتالوگ کالا» و «انتخاب مشتری» در `InvoiceCreatorView.jsx`:** دو دکمه جدید برای باز کردن مدال‌های کاتالوگ کالاها (`showCatalogModal`) و فهرست مشتریان (`showCustomerModal`) اضافه شد تا کاربران بتوانند اقلام و مشتریان ثبت‌شده را با یک کلیک به فاکتور اضافه کنند.
9. **اتصال دکمه‌های «ابزارهای هوشمند» و «جستجوی سراسری» در `Navbar.jsx` و `Sidebar.jsx`:** دکمه آچار (`Wrench`) جهت باز کردن پنجره ۱۵ قابلیت هوشمند (`SmartToolsModal`) و دکمه جستجو (`Search`) در هدر و منوی کناری اضافه شدند.
10. **تکمیل شمای دیتابیس فلاتر مطابق `DATABASE.md`:** جداول مفقوده شامل `InvoiceItemsTable` (اقلام فاکتور)، `PaymentsTable` (تراکنش‌های مالی)، `ExpensesTable` (هزینه‌ها)، `IncomeTable` (درآمدها) و `BackupsTable` (پشتیبان‌ها) در فایل `lib/database/app_database.dart` پیاده‌سازی شدند.
11. **اتصال پرووایدرهای Riverpod فلاتر به دیتابیس Drift/SQLite:** متدهای کمکی `persistInvoiceRecord`، `persistCustomerRecord` و `persistProductRecord` در کلاس `AppDatabase` اضافه شدند و تمام پرووایدرهای اصلی (`InvoiceListNotifier`، `CustomerListNotifier` و `ProductListNotifier`) جهت ذخیره‌سازی آفلاین واقعی به دیتابیس متصل شدند.
12. **حذف هاردکد بودن `customerId` و `cardNumber` در فلاتر (`invoice_create_screen.dart`):** هنگام ثبت فاکتور جدید، شناسه مشتری به صورت پویا از لیست مشتریان بازیابی یا تولید می‌شود، شماره کارت از حساب کسب‌وکار خوانده می‌شود و در صورت غیرنقدی بودن پرداخت، مانده بدهی مشتری در پرووایدر به‌روزرسانی می‌شود.
13. **اصلاح باگ درصد تخفیف در فلاتر (`invoice_provider.dart`):** مقدار `discountPercent` در فاکتور نمونه شماره `1003` از ۱۰ درصد به **۵ درصد** صحیح اصلاح شد.
14. **اصلاح فیلتر آمار امروز در داشبورد فلاتر (`dashboard_screen.dart`):** محاسبه متغیرهای `todaySales` و `todayReceived` به فاکتورهای امروز (`inv.date == JalaliHelper.getTodayJalali()`) مقید شد.

با انجام موفقیت‌آمیز این اصلاحات، خروجی بیلد تولیدی نسخه وب (`npm run build`) بدون هیچ‌گونه خطا کامپایل شده و هر دو لایه اپلیکیشن در بالاترین سطح پایداری و کارایی قرار گرفته‌اند.

