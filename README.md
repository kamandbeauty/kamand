<div align="center">

# جاوید

**نرم‌افزار حسابداری کسب‌وکارهای ایرانی — آفلاین-اول، فارسی، متن‌باز**

[![tests](https://img.shields.io/badge/آزمون‌ها-۳۶۴%20موفق-16a34a)](packages/core/test)
[![core](https://img.shields.io/badge/هسته-TypeScript-0f766e)](packages/core)
[![offline](https://img.shields.io/badge/آفلاین--اول-✓-16a34a)](#تعهد-دوم-بدون-اینترنت-هم-کار-میکند)
[![pwa](https://img.shields.io/badge/PWA-قابل%20نصب-7c3aed)](#نصب-روی-گوشی)
[![size](https://img.shields.io/badge/حجم-۱۰۳KB%20gzip-2563eb)](apps/web)

</div>

---

## چرا جاوید؟

جاوید پس از بررسی دقیق بازار اپلیکیشن‌های حسابداری موبایل ایران ساخته شد. مطالعهٔ رقبا و
مهم‌تر از آن **شکایت‌های واقعی کاربرانشان**، سه مشکل تکرارشونده را نشان داد:

| مشکل رایج در بازار | پاسخ جاوید |
|---|---|
| با پایان اشتراک، دسترسی به دفتر حساب مشتریان قطع می‌شود | **دادهٔ شما هرگز گروگان گرفته نمی‌شود** |
| وابستگی کامل به سرور؛ قطعی اینترنت یعنی توقف فروش | **آفلاین-اول** |
| چندپلتفرمی شدن با بازنویسی از صفر و افت کیفیت | **منطق دامنه مستقل از رابط کاربری** |

این سه، نه «قابلیت» بلکه **تعهد معماری** هستند و در کد قفل شده‌اند
(`subscription.ts`، `sync.ts` و جدایی پکیج `core`). آزمون‌های
[`commitments.test.ts`](packages/core/test/commitments.test.ts) نقض آن‌ها را
به صورت خودکار می‌گیرند.

> 📄 تحلیل کامل بازار: [`docs/aino-feature-analysis.md`](docs/aino-feature-analysis.md)
> 📄 ریشه‌یابی مشکلات رقبا: [`docs/aino-problems-and-opportunities.md`](docs/aino-problems-and-opportunities.md)

---

## تعهد اول: دادهٔ شما متعلق به شماست

بزرگ‌ترین شکایت کاربران نرم‌افزارهای مشابه این است که با تمام شدن اشتراک،
به اطلاعات **خودشان** دسترسی ندارند. در جاوید:

```
پس از انقضای اشتراک:
  ✅ مشاهده، جستجو، گزارش‌گیری، چاپ، خروجی Excel/JSON  →  همیشه آزاد
  ⛔ فقط ثبت رکورد جدید                                  →  قفل
```

به‌علاوه: هشدار در ۱۴، ۷، ۳ و ۱ روز مانده به انقضا، و **۷ روز مهلت ارفاق** پس از آن.
دکمهٔ «دریافت کل اطلاعات» در تنظیمات، بدون هیچ شرطی همیشه فعال است.

## تعهد دوم: بدون اینترنت هم کار می‌کند

مغازه‌دار وسط فروش نباید با قطعی اینترنت متوقف شود.

- همهٔ نوشتن‌ها ابتدا **محلی** ذخیره می‌شوند
- شناسه‌ها **UUID سمت کلاینت** هستند تا رکورد بدون تماس با سرور ساخته شود
- **ساعت لامپورت** ترتیب علّی رویدادها را مستقل از ساعت دستگاه حفظ می‌کند
- حل تعارض **قطعی‌گرا** است: نتیجه روی همهٔ دستگاه‌ها یکسان درمی‌آید
- ارسال مجدد پس از قطعی **بی‌خطر** است — رکورد تکراری ساخته نمی‌شود

همگام‌سازی **کاملاً اختیاری** است: برنامه بدون هیچ سروری کامل کار می‌کند.

## تعهد سوم: هسته مستقل از رابط کاربری

پکیج `@javid/core` هیچ وابستگی‌ای به مرورگر، React یا پایگاه داده ندارد.
همان هسته می‌تواند پشت وب، موبایل یا دسکتاپ بنشیند — بدون بازنویسی.

---

## امکانات

<table>
<tr><td width="50%" valign="top">

**🧾 فاکتور**
- فروش، خرید، برگشت از فروش/خرید، ضایعات، پیش‌فاکتور
- رسمی و غیررسمی با مالیات بر ارزش افزوده
- سرشکن کردن دقیق تخفیف کلی بین سطرها
- محاسبهٔ سود هر فاکتور
- تبدیل پیش‌فاکتور و برگشت جزئی با بهای اصلی
- چاپ با مبلغ به حروف فارسی

**📦 کالا و انبار**
- کالا و خدمت، بارکد، دو واحدی با ضریب تبدیل
- کاردکس با مانده تجمعی
- هشدار حداقل موجودی

</td><td width="50%" valign="top">

**👥 اشخاص**
- مشتری، فروشنده، سهامدار (با درصد شراکت)
- صورتحساب با مانده تجمعی

**💳 خزانه و چک**
- صندوق، بانک، تنخواه‌گردان، انتقال
- چرخهٔ کامل چک: در جریان → وصول / برگشت

**📊 نُه گزارش استاندارد**
سود و زیان · ترازنامه · تراز آزمایشی · بدهکاران و بستانکاران ·
موجودی کالا · دفتر روزنامه · مرور حساب‌ها · صورتحساب سرمایه · دفتر حساب‌ها

**🏛 سامانهٔ مؤدیان**
- شمارهٔ منحصربه‌فرد مالیاتی ۲۲ کاراکتری با رقم کنترلی Verhoeff
- صورتحساب الکترونیکی نوع اول و دوم
- اصلاحیه، ابطالیه و برگشت از فروش
- گزارش دوره‌ای مالیات بر ارزش افزوده
- اصلاحیه و ابطالیه با ارجاع به اصلی

**🖨 چاپ و اسکن**
- چاپ حرارتی ESC/POS با شکل‌دهی صحیح حروف فارسی
- بلوتوث، USB یا دریافت فایل
- بارکدخوان سخت‌افزاری و دوربین

**🏪 چند کسب‌وکاره**
- هر کسب‌وکار با اطلاعات کاملاً جدا
- جابه‌جایی سریع از نوار کناری

**☁️ همگام‌سازی چنددستگاهی**
- ورود با شمارهٔ موبایل و کد یک‌بارمصرف
- چند کاربر با سطح دسترسی مجزا
- همگام‌سازی افزایشی و خودکار

**📱 نصب روی گوشی**
- PWA با آیکن اختصاصی و اجرای تمام‌صفحه
- کارکرد کامل بدون اینترنت

**📜 ممیزی و کنترل**
- ردّ ممیزی خودکار با تفاوت‌گیری فیلدها
- قفل دورهٔ مالی پس از بستن سال
- نقش و اجازهٔ دقیق برای هر کاربر

**📒 دفتر و اسناد**
- سند دستی با اعتبارسنجی توازن
- مانده‌های اول دوره و سند افتتاحیه

**🔒 بستن سال مالی**
- سند اختتامیه، تسهیم سود و افتتاحیهٔ سال بعد
- پیش‌نمایش کامل پیش از هر تغییر
- بررسی سلامت دفتر

</td></tr>
</table>

**زیرساخت:** تاریخ شمسی در همه‌جا · ارقام فارسی · راست‌به‌چپ · ذخیره‌سازی IndexedDB ·
خروجی CSV/Excel/JSON · چاپ A4 و حرارتی · مهاجرت خودکار داده

---

## معماری

```
packages/core/          هستهٔ حسابداری — بدون وابستگی به UI
  money.ts              پول صحیح (ریال)، گرد کردن بانکی، مبلغ به حروف
  jalali.ts             تقویم جلالی بدون کتابخانهٔ خارجی
  ledger.ts             ⭐ موتور سند دوطرفه — تضمین توازن
  inventory.ts          ⭐ بهای تمام‌شده: FIFO / LIFO / میانگین موزون
  invoice.ts            محاسبات فاکتور و اعتبارسنجی
  posting.ts            تبدیل رویداد کسب‌وکار به سند متوازن
  reports.ts            نُه گزارش، همه مشتق از دفتر
  tax.ts                ⭐ سامانهٔ مؤدیان — Verhoeff، شمارهٔ مالیاتی، صورتحساب
  tax-transport.ts      نرمال‌سازی، امضا و رمزگذاری بستهٔ ارسالی
  audit.ts              ردّ ممیزی و قفل دورهٔ مالی
  closing.ts            بستن سال مالی و بررسی سلامت دفتر
  workspace.ts          چند کسب‌وکاره، نقش و جداسازی داده
  escpos.ts             چاپ حرارتی + شکل‌دهی حروف فارسی
  subscription.ts       🔒 تعهد ۱
  sync.ts               🔒 تعهد ۲
  export.ts             خروجی CSV / Excel / پشتیبان کامل

  protocol.ts           قرارداد مشترک کلاینت و سرور

apps/web/               رابط کاربری React + Vite
  storage.ts            IndexedDB → localStorage → حافظه
  printer.ts            بلوتوث / USB / فایل
  barcode.ts            اسکنر سخت‌افزاری و دوربین
  api.ts                کلاینت HTTP
  syncEngine.ts         ادغام و حل تعارض سمت کلاینت
  pwa.ts                نصب و سرویس‌ورکر
  workspaces.ts         چند کسب‌وکاره و مهاجرت خودکار

apps/server/            سرور همگام‌سازی (Node + node:sqlite)
  db.ts                 دفتر تغییرات فقط-افزودنی
  auth.ts               کد یک‌بارمصرف، نشست، نقش
  sync.ts               ⭐ push / pull / snapshot
  http.ts               لایهٔ HTTP بدون فریم‌ورک
  sms.ts                آداپتور پیامک (کاوه‌نگار، ملی‌پیامک، وب‌هوک)
  tax-crypto.ts         امضا و رمزگذاری سامانهٔ مؤدیان
  tax-client.ts         ارسال و استعلام صورتحساب
```

### اصول طراحی

**۱ — پول هرگز اعشاری نیست.** همهٔ مبالغ عدد صحیح ریال هستند. گرد کردن
نیم-زوج (banker's rounding) از سوگیری انباشته جلوگیری می‌کند، و `allocate()`
تضمین می‌کند هنگام سرشکن کردن تخفیف **حتی یک ریال گم نشود**.

**۲ — هیچ گزارشی ذخیره نمی‌شود.** همهٔ گزارش‌ها در لحظه از `JournalEntry` و
`StockMovement` مشتق می‌شوند. نتیجه: گزارش هرگز با واقعیت ناهماهنگ نمی‌شود.

**۳ — سند نامتوازن وارد دفتر نمی‌شود.** `EntryBuilder` پیش از ساخت هر سند،
توازن را بررسی می‌کند و در صورت نقض `UnbalancedEntryError` پرتاب می‌کند.

**۴ — تاریخ میلادی ذخیره، شمسی نمایش.** داده به ISO ذخیره می‌شود و فقط در
لحظهٔ نمایش به جلالی تبدیل می‌گردد.

---

## شروع

```bash
npm install
npm test          # ۳۶۴ آزمون
npm run dev       # اجرای برنامهٔ وب
npm run server    # اجرای سرور همگام‌سازی (اختیاری)
npm run build     # ساخت نسخهٔ تولید
```

سرور روی پورت ۸۷۸۷ بالا می‌آید. برای ذخیره‌سازی دائمی:

```bash
PORT=8787 JAVID_DB=./javid.sqlite npm run server
```

سپس در برنامه به «حساب و همگام‌سازی» بروید و آدرس سرور را وارد کنید.

### نصب روی گوشی

جاوید یک PWA است. پس از باز کردن در مرورگر، از تنظیمات دکمهٔ «نصب جاوید»
را بزنید یا از منوی مرورگر «افزودن به صفحهٔ اصلی» را انتخاب کنید.

### ارسال پیامک

بدون پیکربندی، کد تأیید در لاگ سرور چاپ می‌شود. برای ارسال واقعی:

```bash
SMS_PROVIDER=kavenegar KAVENEGAR_API_KEY=... npm run server
# یا: SMS_PROVIDER=melipayamak | webhook
```

## آزمون‌ها

```
هسته                    ۲۰۶
  پول، تاریخ جلالی، سند دوطرفه، فاکتور، موجودی
  تعهدات معماری (۱۹)، سامانهٔ مؤدیان (۳۶)، چاپ فارسی (۱۹)
  ممیزی و دسترسی (۳۷)، بستن سال و سلامت دفتر (۳۴)
  سناریوی واقعی مغازه (۱۴)

سرور                    ۱۰۰
  همگام‌سازی، حل تعارض، اتمی بودن، احراز هویت، دسترسی
  نرمال‌سازی و امضای سامانه، رمزگذاری، پیامک

وب                       ۵۸
  ذخیره‌سازی، بارکد، PWA، چند کسب‌وکاره
  همگام‌سازی سرتاسری دو دستگاه
────────────────────────────
جمع                     ۳۶۴
```

سه آزمون از بقیه مهم‌ترند:

**سناریوی واقعی مغازه** — پس از خرید، خرید دوم، فروش، دریافت وجه و برگشت،
**تراز آزمایشی باید متوازن بماند** و **دارایی = بدهی + سرمایه** برقرار باشد.

**همگام‌سازی سرتاسری** — دو دستگاه واقعی روی یک سرور واقعی: فاکتوری که روی
گوشی زده می‌شود روی تبلت دیده می‌شود، ۱۵۰ فاکتور آفلاین یکجا همگام می‌شوند،
و ارسال مجدد پس از قطعی داده را دوباره نمی‌سازد.

**تعهدات معماری** — اگر کسی روزی `export` را پشت اشتراک قفل کند، قرمز می‌شود.

**رمزنگاری سامانهٔ مؤدیان** — امضا واقعاً با کلید عمومی راستی‌آزمایی می‌شود و
صورتحساب رمزشده واقعاً رمزگشایی شده و با اصل مقایسه می‌گردد؛ نه صرفاً
بررسی اینکه تابع خطا نداده است.

---

## نقشهٔ راه

- [x] هستهٔ حسابداری دوطرفه + بهای تمام‌شده
- [x] رابط کاربری وب فارسی
- [x] خروجی CSV / Excel / پشتیبان کامل
- [x] ذخیره‌سازی IndexedDB با سقوط تدریجی
- [x] **اتصال به سامانهٔ مؤدیان** (بزرگ‌ترین شکاف رقبا)
- [x] چاپ حرارتی ESC/POS با فارسی صحیح
- [x] بارکدخوان سخت‌افزاری و دوربین
- [x] بک‌اند و همگام‌سازی چنددستگاهی
- [x] احراز هویت با شمارهٔ موبایل
- [x] لایهٔ انتقال سامانهٔ مؤدیان (نرمال‌سازی، امضا، رمزگذاری)
- [x] ارسال پیامک کد تأیید
- [x] نصب روی گوشی (PWA)
- [ ] فعال‌سازی ارسال واقعی — نیازمند گواهی امضا از مرکز میانی معتبر
- [ ] نسخهٔ بومی موبایل

---

<div align="center">
<sub>ساخته‌شده با تمرکز بر یک اصل: دفتر حساب کسب‌وکار شما، مال شماست.</sub>
</div>
