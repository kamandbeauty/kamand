# Copy this file to android/key.properties for local signing.
# android/key.properties is ignored by Git; never commit it.
# The storeFile path is relative to the android/ directory unless absolute.

storeFile=upload-keystore.jks
storePassword=REPLACE_WITH_YOUR_STORE_PASSWORD
keyAlias=upload
keyPassword=REPLACE_WITH_YOUR_KEY_PASSWORD
