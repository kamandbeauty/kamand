# Add project specific ProGuard rules here.
# Room, Hilt, and Compose ship their own consumer rules, so this file is
# intentionally minimal for Phase 1 (isMinifyEnabled = false in debug/release
# for now — this is Toman-critical financial software, so minification will
# be turned on deliberately and tested against the full test suite later).
