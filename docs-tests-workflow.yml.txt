name: Tests & Analyze

on:
  push:
    branches:
      - main
      - "arena/**"
  pull_request:
  workflow_dispatch:

jobs:
  test:
    name: Flutter analyze + test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Install sqlite3 native library (for Dart host tests)
        run: |
          sudo apt-get update
          sudo apt-get install -y libsqlite3-dev

      - name: Set up Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Get Flutter dependencies
        run: flutter pub get

      - name: Verify formatting
        run: dart format --output=none --set-exit-if-changed .
        continue-on-error: true

      - name: Analyze project source
        run: flutter analyze --no-pub --no-fatal-warnings

      - name: Run unit tests
        run: flutter test --reporter expanded

# ─── راهنمای فعال‌سازی (توسط مالک مخزن) ───
# ۱) در گیت‌هاب فایل docs-tests-workflow.yml.txt را باز کنید و محتوای بالای این خط را کپی کنید
# ۲) فایل جدیدی به مسیر .github/workflows/tests.yml بسازید و محتوا را در آن بچسبانید
# ۳) Commit — از این پس همهٔ ۷۸ تست مالی + تحلیل ایستا روی هر PR اجرا می‌شود
#
# نکته: برای اجرای CI روی این PR هم می‌توانید از تب Actions ← Build Ruby Factor ←
# Run workflow ← برنچ arena/01a03db6-kamand استفاده کنید.
